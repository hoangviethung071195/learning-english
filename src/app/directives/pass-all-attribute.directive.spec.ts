import { PassAllAttributeDirective } from './pass-all-attribute.directive';

describe('PassAllAttributeDirective', () => {
  it('should create an instance', () => {
    const directive = new PassAllAttributeDirective();
    expect(directive).toBeTruthy();
  });
});
